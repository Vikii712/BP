window.disableImages = function(enabled) {
    document.body.classList.toggle("disable-images", enabled);
}

window.highlightLinks = function(enabled) {
    document.body.classList.toggle("highlight-links", enabled);
}

